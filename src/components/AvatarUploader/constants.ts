export namespace AvatarCropperComponent {
  export const changePhotoButtonText = 'Change Photo';
  export const allowedExtension = ['jpg', 'png', 'jpeg'];
  export const inputId = 'nabi-image-cropper';
  export const maxImageSize = 10000000; // in bytes
  export const errorMessage = 'The file extension must be png, jpg or jpeg, and its size cannot exceed 10MB.';
}
