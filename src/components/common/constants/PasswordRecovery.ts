export namespace PasswordRecoveryComponent {
  export const pageTitle = 'Password Recovery';
}

export namespace PasswordRecoveryFormComponent {

  export enum Text {
    PasswordRecovery = 'Submit'
  }

  export enum Placeholders {
    Email = 'Email address'
  }

  export enum FieldNames {
    Email = 'email'
  }

  export enum Ids {
    Email = 'email',
  }
}
