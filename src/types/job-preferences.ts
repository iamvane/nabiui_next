export interface LessonSizeType {
    oneStudent: boolean;
    smallGroups: boolean;
    largeGroups: boolean;
}

export interface AgeGroupType {
    children: boolean;
    teens: boolean;
    adults: boolean;
    seniors: boolean;
}
